{
  "name": "midi-pad-controller",
  "version": "1.0.0",
  "description": "Controlador MIDI com pads para reprodução de músicas",
  "scripts": {
    "start":   "node server.js",
    "icons":   "node generate-icons.js",
    "sync":    "npx cap sync",
    "android": "npx cap open android",
    "build":   "node generate-icons.js && npx cap sync"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core":    "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
